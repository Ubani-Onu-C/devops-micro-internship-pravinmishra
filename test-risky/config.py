# Sample configuration file for testing pre-commit hook
# (secret and debug statement removed after hook verification)

def connect_to_service():
    # Configuration is now loaded from environment variables, not hardcoded
    return True
